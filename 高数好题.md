题目(考察罗尔定理): 
设 $f(x)$ 在 $[a, b]$ 上连续，在 $(a, b)$ 内可导，$f(a) = 0$，$a > 0$，证明：存在 $\xi \in (a, b)$，使得：
$$f(\xi) = \frac{b - \xi}{a} f'(\xi)$$
分析: 
先将结论变形改写为：
$$f'(\xi) - \frac{a}{b - \xi} f(\xi) = 0$$
对照标准求导公式 $f'(x) + f(x)\varphi'(x)$，由此构造辅助函数：
$$F(x) = f(x) e^{\int \left(-\frac{a}{b-x}\right) \mathrm{d}x} = f(x)(b - x)^a$$
再利用罗尔定理（Rolle's Theorem）即可证明：
1. $F(a) = f(a)(b - a)^a = 0$
2. $F(b) = f(b)(b - b)^a = 0$
证明: 
令 $F(x) = f(x)(b - x)^a$。
显然 $F(x)$ 在 $[a, b]$ 上连续，在 $(a, b)$ 内可导，且 $F(a) = F(b) = 0$, (这一步很关键，这是罗尔定理的应用条件)
由罗尔定理可知，存在 $\xi \in (a, b)$，使得 $F'(\xi) = 0$，即：
$$f'(\xi)(b - \xi)^a - a f(\xi)(b - \xi)^{a-1} = 0$$
已知 $a > 0$ 且 $\xi \in (a, b)$（即 $b - \xi \neq 0$），两边同除以 $(b - \xi)^{a-1}$ 得：
$$f'(\xi)(b - \xi) - a f(\xi) = 0$$
整理可得：
$$f(\xi) = \frac{b - \xi}{a} f'(\xi)$$
思路拓展: 
上面是证明一阶导数为 $0$（即使用一次罗尔定理）的问题。但有些题目涉及到二阶导数为 $0$，即需要多次使用罗尔定理。
- **难点**：要找到函数值相等的三个不同点，即 $f(a) = f(b) = f(c)$（不妨设 $a < b < c$）。
- **应用**：
    1. 分别在 $[a, b]$ 和 $[b, c]$ 上使用罗尔定理，可知存在 $\xi_1 \in (a, b)$ 和 $\xi_2 \in (b, c)$，使得：
$$f'(\xi_1) = f'(\xi_2) = 0$$
    2. 进而在 $[\xi_1, \xi_2]$ 上再对 $f'(x)$ 使用一次罗尔定理，即可得：
$$f''(\xi) = 0 \quad \left(\text{其中 } \xi \in (\xi_1, \xi_2) \subset (a, c)\right)$$


题目: 
设函数 $f(x)$ 在区间 $[0, 1]$ 上具有二阶导数，且 $f(1) > 0$，$\lim_{x \to 0^+} \frac{f(x)}{x} < 0$。
证明：方程 $f(x)f''(x) + [f'(x)]^2 = 0$ 在区间 $(0, 1)$ 内至少存在两个不同实根。
分析: 
辅助函数为 $F(x) = f(x) \cdot f'(x)$，即证：

$$\begin{cases} F'(\xi_1) = 0 \\ F'(\xi_2) = 0 \end{cases}$$

其中 $\xi_1$ 与 $\xi_2$ 不相等。
证明: 
由题设知 $f(x)$ 连续且 $\lim_{x \to 0^+} \frac{f(x)}{x}$ 存在，所以 $f(0) = 0$。
又由连续零点定理知 $f(b) = 0$（其中 $b \in (0, 1)$）。
由罗尔定理知，存在 $c \in (0, b) \subset (0, 1)$，使得：
$$f'(c) = 0$$
令 $F(x) = f(x)f'(x)$，由题设知 $F(x)$ 在区间 $[0, b]$ 上可导，且：
$$F(0) = 0, \quad F(c) = 0, \quad F(b) = 0$$
根据罗尔定理，存在 $\xi \in (0, c)$，$\eta \in (c, b)$，使得：
$$F'(\xi) = F'(\eta) = 0$$
注意到 $F'(x) = [f(x)f'(x)]' = f(x)f''(x) + [f'(x)]^2$。
即 $\xi, \eta$ 是方程 $f(x)f''(x) + [f'(x)]^2 = 0$ 在区间 $(0, 1)$ 内的两个不同实根。

感悟: 这里的一个极限的条件透漏了非常多的信息, 所以在证明证明题的时候, 可以对条件组合式的分析得到一些小的条件;

题目: 
设函数 $f(x)$ 满足 $f(0) = 0$，且当 $x > 0$ 时，$f(x) < 0$，$f'(x) < 0$，$f''(x) > 0$，则当 $0 < a < x < b$ 时，有（ $\quad$ ）。
选项: 
(A) $x f(x) > a f(a)$
(B) $b f(b) > x f(x)$
(C) $x f(a) > a f(x)$
(D) $x f(b) > b f(x)$
解析: 
**第一步：分析 $g(x) = x f(x)$ 的单调性**
令 $g(x) = x f(x)$（$x > 0$），对其求导得：
$$g'(x) = f(x) + x f'(x)$$
因为当 $x > 0$ 时，$f(x) < 0$ 且 $f'(x) < 0$，所以：
$$g'(x) < 0$$
由此可知 $g(x)$ 在 $(0, +\infty)$ 上**单调递减**。
当 $0 < a < x < b$ 时，由单调性可得：
$$g(b) < g(x) < g(a)$$
即：
$$b f(b) < x f(x) < a f(a)$$
据此可知：
- **选项 (A) 错误**（应为 $x f(x) < a f(a)$）
- **选项 (B) 错误**（应为 $b f(b) < x f(x)$）
**第二步：分析 $h(x) = \frac{f(x)}{x}$ 的单调性**
再令 $h(x) = \frac{f(x)}{x}$（$x > 0$），对其求导得：
$$h'(x) = \frac{x f'(x) - f(x)}{x^2}$$
> 分子中 $x f'(x)$ 为负，$f(x)$ 为负，直接相减无法直接判断 $f$ 与 $x f'$ 的大小关系，此时联想到**拉格朗日中值定理**。

利用 $f(0) = 0$，将 $f(x)$ 展开为 $f(x) - f(0)$，代入可得：
$$h'(x) = \frac{x f'(x) - [f(x) - f(0)]}{x^2} = \frac{x f'(x) - f'(\xi) \cdot x}{x^2} = \frac{f'(x) - f'(\xi)}{x}$$
其中 $\xi \in (0, x)$。
因为 $f''(x) > 0$，所以导函数 $f'(x)$ 在区间上**单调增加**。
由于 $0 < \xi < x$，故有：
$$f'(x) > f'(\xi) \implies f'(x) - f'(\xi) > 0$$
又因为 $x > 0$，所以：
$$h'(x) > 0$$
由此可知 $h(x)$ 在 $(0, +\infty)$ 上**单调增加**。
**第三步：得出结论**
当 $0 < a < x < b$ 时，由 $h(x)$ 的单调递增性可得：
$$h(a) < h(x) < h(b)$$
即：
$$\frac{f(a)}{a} < \frac{f(x)}{x} < \frac{f(b)}{b}$$
- 观察左侧不等式：$\frac{f(a)}{a} < \frac{f(x)}{x} \implies a f(x) > x f(a)$，即 $x f(a) < a f(x)$，故**选项 (C) 错误**。
- 观察右侧不等式：$\frac{f(x)}{x} < \frac{f(b)}{b} \implies b f(x) < x f(b)$，即 $x f(b) > b f(x)$，故**选项 (D) 正确**。